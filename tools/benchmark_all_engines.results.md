# CrispEmbed engine benchmark

`crispembed` CLI, median of 1 runs. `load` = model load+init (`--dim`); `encode` = marginal encode (`total - load`).

| engine | modality | gguf | load (ms) | encode (ms) | total (ms) |
|---|---|---|---:|---:|---:|
