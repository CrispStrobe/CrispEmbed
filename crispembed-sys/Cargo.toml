[package]
name = "crispembed-sys"
version = "0.1.0"
edition = "2021"
description = "Raw FFI bindings to the crispembed C library"
links = "crispembed"
build = "build.rs"

[features]
cuda   = []
metal  = []
vulkan = []

[build-dependencies]
cmake = "0.1"
